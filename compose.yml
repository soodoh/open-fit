services:
  postgres:
    image: postgres:latest
    container_name: postgres
    restart: always
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - postgres:/var/lib/postgresql/data
    networks:
      - openfit-net

volumes:
  postgres:

networks:
  openfit-net:
